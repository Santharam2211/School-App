import { DefaultTheme } from 'react-native-paper';

const theme = {
  ...DefaultTheme,
  roundness: 12,
  colors: {
    ...DefaultTheme.colors,
    primary: '#ff7a00',
    secondary: '#000000',
    accent: '#ff7a00',
    background: '#ffffff',
    surface: '#ffffff',
    placeholder: '#666666',
    disabled: '#bdbdbd',
    text: '#000000',
    notification: '#ff7a00',
  },
};

export default theme;


